/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["img.icons8.com","seeklogo.com", "nodejs.org", "upload.wikimedia.org","cdn.sanity.io"]
    }
}

module.exports = nextConfig
